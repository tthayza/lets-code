console.log('--- PROGRAMA ---')

// Entrada (recebimento de dados)
let nome = prompt('insira seu nome').toUpperCase()

// Processamento (tratamento, formatação, transformação de dados)
nome = nome.toUpperCase()

// Saída (exibição dos dados)
document.write(nome)
